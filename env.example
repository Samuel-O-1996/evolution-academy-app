# Copy these into Vercel -> Project -> Settings -> Environment Variables.
# Keep DEMO on until Moodle has real courses AND per-user auth is in place.
NEXT_PUBLIC_DEMO_MODE=true

# Used only when NEXT_PUBLIC_DEMO_MODE=false (live data via the server proxy):
MOODLE_URL=https://learn.evolutionacademy.com.ng
MOODLE_TOKEN=put_your_web_service_token_here
